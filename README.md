# Interactive Portfolio

An interactive 3D portfolio experience where visitors explore a virtual room and discover my creative projects through clickable objects.

## Setup

Make sure to install dependencies:

```bash
# pnpm
pnpm install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# pnpm
pnpm dev
```